<% content_for :title, "Edit listing" %>

<%= render Website::SectionComponent.new do %>
  <div class="mx-auto w-full md:w-2/3">
    <h1 class="text-4xl font-bold">Edit listing</h1>

    <p class="mt-3 text-sm text-slate-600 dark:text-slate-300">
      Updates from listing owners are sent for admin approval before they are publicly shown.
    </p>

    <%= render "form", directory_listing: @directory_listing %>

    <div class="mt-6 flex flex-col gap-3 sm:flex-row sm:items-center">
      <%= render Website::ButtonComponent.new(
        text: "Back to listing",
        path: directory_path(@directory_listing),
        variant: :outline
      ) %>
    </div>

  </div>
<% end %>